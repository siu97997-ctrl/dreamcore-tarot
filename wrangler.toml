name = "dreamcore-tarot"
main = "./worker/index.ts"
compatibility_date = "2026-08-12"
compatibility_flags = ["nodejs_compat"]

[vars]
NODE_ENV = "production"

[assets]
directory = "./public"

# 备注：DEEPSEEK_API_KEY 请通过 Cloudflare 控制台的
# Settings → Variables and Secrets 添加为 Secret 变量
# （不要写在此文件中，避免泄露到 GitHub）
