# 2026-08-12

## dreamcore-tarot 塔罗牌网站 — 部署到腾讯云 CloudBase 云托管（完成）

- 平台：腾讯云 CloudBase 云托管（容器型服务），环境 ID `dreamcore-tarot-d5fv5qk40c38b7f7`，有效期至 2027-02-12（免费体验版 6 个月）。
- 线上域名：`https://dreamcore-tarot-295511-11-1466810715.sh.run.tcloudbase.com`（国内可访问，Tencent 已统一备案）。
- 部署包：`/Users/mimiya/Desktop/g-p-tarot/dreamcore-tarot-cloudbase.zip`（Next.js/vinext 框架 + Dockerfile，`next.config.ts` 设 `output:"standalone"`，容器端口 80）。
- 部署方式：用户在控制台「云托管」上传 zip 后点「部署」（命令行 `tcb cloudrun deploy` 因连接器 OAuth 凭证加密、MCP 工具未注入会话而改用控制台点击）。
- 验证结果：首页 HTTP 200；`POST /api/readings` 调通并返回 DeepSeek 解读（`status:completed`，确认 `DEEPSEEK_API_KEY` 环境变量已生效）；牌面图 `/cards_corrected/...` HTTP 200。
- 关键结论：`app/chatgpt-auth.ts` 是死代码（`requireChatGPTUser`/`getChatGPTUser` 全站未被调用），普通访客无需 ChatGPT 登录即可使用。
- 本地构建验证曾放到 `/tmp/cb_extract`（`npm ci` + `vinext build` 成功产出 `dist/standalone/server.js`）。
- 注意事项：免费体验版首次访问冷启动约 20s；不要超额使用或购买付费资源包/自动续费。如需自定义域名需自行 ICP 备案。

## 连接器状态
- 用户已连接「腾讯云 CloudBase」连接器，但其 OAuth 凭证为 aes-256-gcm 加密、未注入 `tcb` CLI，且 MCP 部署工具未注入本会话，故无法用 CLI 直推，最终走控制台部署。
